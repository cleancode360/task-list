apiVersion: v1
kind: ConfigMap
metadata:
  name: backend-config
  namespace: todo-app
data:
  SPRING_DATASOURCE_URL: jdbc:postgresql://REPLACE_RDS_ENDPOINT:5432/todo
  APP_CORS_ALLOWED_ORIGINS: https://REPLACE_AMPLIFY_DOMAIN
